import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        borderRadius: 5,
        padding: 10,
        margin: 5,
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        fontWeight: 'bold',
    }
});